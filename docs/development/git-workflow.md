# Git 工作流

1. 从目标基线创建小范围分支并检查现有工作区。
2. 保留他人的未提交修改；生成器执行前先提交、暂存或备份。
3. 页面变更同步审查 route-map、资源 JSON、API、类型和权限编码。
4. 提交前检查敏感信息、构建产物、`git diff --check` 与 `npm run build`。
5. PR 说明影响领域、运行模式、资源变化、验证结果和未关闭风险。

